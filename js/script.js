// declare variables to be 
let term = ""
let addends = ""
let subtrahend = ""

// addition function
function addition (term, addends) {
  sum =  term + addends;
  return sum;
}

// subtracion function
function subtraction (term, subtrahend) {
  difference = term - subtrahend;
  return difference;
}

// multiplication function
function product (term, multiplicand) {
  product = term * multiplicand;
  return product;
}

// division function 
function division (term, denominator) {
  ratio = term / denominator;
  return ratio;
}

// operator function, takes an operator and 2 numbers and calls the relevant function
// on the numbers

